#include <bits/stdc++.h>

using ll = long long;

ll a, b;

int main()
{
    std::cin.tie(nullptr)->sync_with_stdio(false);

    std::cin >> a >> b;
    std::cout << a * b - a - b << "\n";
}